import { PagePlaceholder } from "@/components/PagePlaceholder";

// NOTE: The real Add Reservation flow is built in P6.1 by another agent; this is
// a placeholder so the nav shell (P5.1) is navigable and the tree builds.
export default function AddReservationPage() {
  return (
    <PagePlaceholder title="Add Reservation">
      <p>The multi-product / recurring reservation form will render here (P6.1).</p>
    </PagePlaceholder>
  );
}
