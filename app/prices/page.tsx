import { PagePlaceholder } from "@/components/PagePlaceholder";

// Placeholder — the Update Prices page arrives in a later phase.
export default function PricesPage() {
  return (
    <PagePlaceholder title="Update Prices">
      <p>Per-item price (item_prices) management will render here.</p>
    </PagePlaceholder>
  );
}
