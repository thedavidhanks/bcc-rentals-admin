import { PagePlaceholder } from "@/components/PagePlaceholder";

// Placeholder — the Products management pages arrive in a later phase (admin only).
export default function ProductsPage() {
  return (
    <PagePlaceholder title="Products">
      <p>Product catalog management will render here.</p>
    </PagePlaceholder>
  );
}
