import { PagePlaceholder } from "@/components/PagePlaceholder";

// Placeholder — Categories management arrives in a later phase (admin only).
export default function CategoriesPage() {
  return (
    <PagePlaceholder title="Categories">
      <p>Category management will render here.</p>
    </PagePlaceholder>
  );
}
