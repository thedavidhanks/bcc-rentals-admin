import { PagePlaceholder } from "@/components/PagePlaceholder";

// Placeholder — User & role management arrives in a later phase (admin only).
export default function UsersPage() {
  return (
    <PagePlaceholder title="Users">
      <p>User and role management will render here.</p>
    </PagePlaceholder>
  );
}
