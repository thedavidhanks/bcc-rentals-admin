{
  "name": "@bcc/scheduler",
  "version": "0.0.0",
  "private": true,
  "description": "Shared scheduler + catalog surface for the BCC storefront and admin apps (phase P9 consolidation). Sources are TypeScript consumed via tsconfig paths / transpilePackages — no build step.",
  "exports": {
    ".": "./src/index.ts",
    "./scheduler/errors": "./src/scheduler/errors.ts",
    "./scheduler/policy": "./src/scheduler/policy.ts",
    "./scheduler/types": "./src/scheduler/types.ts",
    "./products/types": "./src/products/types.ts"
  },
  "dependencies": {
    "server-only": "^0.0.1",
    "zod": "^3.24.1"
  }
}
